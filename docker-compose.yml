services:
  client-prod:
    # ... build etc ...
    container_name: client-prod
    ports:
      - "5173:80"  # Redirige le 5173 de l'hôte vers le 80 du conteneur
    networks:
      - commerce-net

  client-test:
    # ...
    container_name: client-test
    ports:
      - "5174:80"
    networks:
      - commerce-net

  dashboard-prod:
    # ...
    container_name: dashboard-prod
    ports:
      - "5175:80"
    networks:
      - commerce-net

  dashboard-test:
    # ...
    container_name: dashboard-test
    ports:
      - "5176:80"
    networks:
      - commerce-net